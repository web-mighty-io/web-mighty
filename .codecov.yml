codecov:
  require_ci_to_pass: true

coverage:
  status:
    client:
      flags: client
    config:
      flags: config
    mighty:
      flags: mighty
    server:
      flags: server
    types:
      flags: types

flags:
  client:
    paths:
      - client/src
  config:
    paths:
      - config/src
  mighty:
    paths:
      - mighty/src
  server:
    paths:
      - server/src
  types:
    paths:
      - types/src
