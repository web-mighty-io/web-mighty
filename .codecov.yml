codecov:
  require_ci_to_pass: yes

coverage:
  status:
    core:
      flags: core
    public:
      flags: public
    server:
      flags: server

flags:
  core:
    paths:
      - core/src
  public:
    paths:
      - public/src
  server:
    paths:
      - server/src